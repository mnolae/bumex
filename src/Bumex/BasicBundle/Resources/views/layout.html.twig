<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
        <title>{% block title %}Demo Bundle{% endblock %}</title>
        <link rel="shortcut icon" href="http://www.gestrafic.com/images/favicon.ico" />
        {% block stylesheets %}
	        <link rel="stylesheet" href="{{ asset('css/demo.css') }}" type="text/css" media="all" />
    		<link type="text/css" href="{{ asset('css/south-street/jquery-ui-1.8.23.custom.css') }}" rel="stylesheet" />
        {% endblock %}

		<script type="text/javascript" src="{{ asset('js/jquery-1.8.0.min.js') }}"></script>
		<script type="text/javascript" src="{{ asset('js/jquery-ui-1.8.23.custom.min.js') }}"></script>
        
    </head>
    <body>
        <div id="symfony-wrapper">
            <div id="symfony-header">
            	<h1>Búmex :: Gestrafic S.L.</h1>
            	<span>&nbsp;Búsqueda masiva de Expedientes</span>
            </div>

            {% if app.session.flash('notice') %}
                <div class="flash-message">
                    <em>Notice</em>: {{ app.session.flash('notice') }}
                </div>
            {% endif %}

            {% block content_header %}
                <ul id="menu">
                    {% block content_header_more %}{% endblock %}
                </ul>

                <div style="clear: both"></div>
				{% block cabecera %}{% endblock %}
            {% endblock %}
            
            

            <div class="symfony-content">
                {% block content %}
                {% endblock %}
            </div>

			

            {% if code is defined %}
                <h2>Code behind this page</h2>
                <div class="symfony-content">{{ code|raw }}</div>
            {% endif %}
        </div>
        {% include 'BumexBasicBundle:Credito:credito.html.twig' %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
